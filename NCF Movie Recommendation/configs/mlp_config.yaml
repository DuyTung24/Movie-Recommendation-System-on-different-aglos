model: "MLP"
layers: [64, 32, 16, 8]  # Tầng đầu tiên = (embedding_user + embedding_item)
lr: 0.001
batch_size: 4096
epochs: 100
num_negatives: 4
patience: 20
top_k: [5, 10, 20]
raw_data_path: "data/raw/ratings.dat"
processed_dir: "data/processed/"
checkpoint_path: "checkpoints/mlp_best.pth"